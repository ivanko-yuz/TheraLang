import { PrimaryImage } from "../primary-image/primary-image.model";
import { Subtitle } from "../subtitle/subtitle.model";
export class TopTitle {
  subtitle: Subtitle;
  primaryImage: PrimaryImage;
  ingress: Subtitle;
}
